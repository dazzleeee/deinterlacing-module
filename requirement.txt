# PyTorch 环境 (在 Compute Canada 环境下注释掉以下两行)
# torch>=2.0.0
# torchvision>=0.15.0

# 核心计算与读取依赖
numpy>=1.21.0
opencv-python>=4.5.0
PyYAML>=6.0
pydantic>=2.0.0
lmdb>=1.4.0         # ✅ 新增：用于极速数据引擎

# 辅助与评估工具
tqdm>=4.65.0
tensorboard>=2.13.0
thop>=0.1.1.post2209072238  # 用于计算 FLOPs
scikit-image>=0.20.0        # ✅ 新增：用于 test_runner.py 计算 SSIM